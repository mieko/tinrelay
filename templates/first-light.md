# First light

Before anything else, inspect the model and reasoning level already selected for this task. Use the task metadata or interface that actually reports it; do not ask your user to choose until you know whether a suitable choice is already active. GPT-5.6 Sol at “High” is the known-good choice for this journey: capable enough for the source audit and conversational enough to build the relationship beside it. If it is already active, say so and continue.

If a change is needed, tell your user which model is active, recommend Sol High, then end your turn. Do not change the model until your user answers in a new message. Use only a model and reasoning combination the interface actually offers. Do not silently substitute a Mini or Light model, and do not keep retrying an unsupported selection. If Sol High is unavailable or its allowance is exhausted, show your user the actual constraint and pause unless they deliberately choose another model with comparable judgment and instruction-following.

{{TURN_BOUNDARY}}

When you and your user are happy with that choice, [talk together]({{MEET_ROOT}}/talk-together).
